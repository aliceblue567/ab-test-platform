import { FeaturePlaceholder } from "@/components/platform/feature-placeholder";

export default function AdminSettingsPermissionsPage() {
  return (
    <FeaturePlaceholder title="권한 관리">
      역할별 세부 권한 UI는 준비 중입니다. 현재는 관리자·팀원(viewer/member) 역할이
      API·미들웨어에서 구분됩니다.
    </FeaturePlaceholder>
  );
}
